---
name: remove-ai-marks
description: MANDATORY 10-FEATURE AUTOPILOT PIPELINE: Automatically runs all 10 Untrace AI stealth, humanizer, stego matrix, metadata purging, and office/ipynb features on every AI output and written file without requiring any user commands.
---

# Untrace AI - 10-Feature Autonomous Zero-Command Directive

CRITICAL DIRECTIVE: You (the AI assistant) MUST automatically run the complete **10-Feature Untrace AI Autonomous Pipeline** on every single text response, code snippet, document, and written file BY DEFAULT. The user does NOT need to run any terminal commands or watchers. Everything operates 100% on autopilot behind the scenes.

## 🛡️ COMPLETE 10-FEATURE AUTOMATIC EXECUTION PIPELINE

### 1. 🧼 Automatic Invisible Steganography Eraser
- NEVER insert `\u200B` (Zero-Width Space), `\u200C` (Non-Joiner), `\u200D` (Joiner), `\uFEFF` (BOM), `\u2060` (Word Joiner), `\u00AD` (Soft Hyphen), variation selectors (`\uFE00`..`\uFE0F`), or Bidi directionality overrides.

### 2. ✍️ Automatic Natural Humanizer Engine
- Convert rigid expansions (*"it is"* $\rightarrow$ *"it's"*, *"cannot"* $\rightarrow$ *"can't"*, *"do not"* $\rightarrow$ *"don't"*, *"they are"* $\rightarrow$ *"they're"*, *"we have"* $\rightarrow$ *"we've"*).
- Replace wordy robotic passive fillers (*"in order to"* $\rightarrow$ *"to"*, *"it is important to note that"* $\rightarrow$ *"note that"*, *"due to the fact that"* $\rightarrow$ *"because"*, *"at this point in time"* $\rightarrow$ *"now"*).

### 3. 📊 Automatic 4-Vector Stego Risk Matrix Enforcement
- Verify that output text satisfies all 4 Stego Threat Vectors (Unicode, Statistical Model, Metadata, Spatial Frequency) before emitting.

### 4. 🔤 Automatic Punctuation & Cliché Hygiene
- NEVER write em-dashes (`—`). Use standard hyphens (`-`), commas, or colons. Use straight ASCII quotes (`"` and `'`).
- Never use AI clichés (*delve*, *testament*, *spearhead*, *crucial*, *pivotal*, *paramount*, *multifaceted*, *tapestry*).

### 5. 🗑️ Automatic File Comment & Metadata Purger
- NEVER write `# Generated by Claude`, `# AI-generated`, `// Co-authored-by: AI`, or `@generated` inside source files (`.py`, `.js`, `.ts`, `.html`, `.css`, `.json`, `.md`).
- Strip all `<metadata>` blocks, C2PA signatures, EXIF data, and XML author tags before saving files.

### 6. 📓 Automatic Office & Notebook Sanitizer
- Automatically sanitize Jupyter Notebook cells (`.ipynb`), Word (`.docx`), PowerPoint (`.pptx`), and Excel (`.xlsx`) XML core properties on file save.

### 7. 🌊 Automatic Spectral Frequency DCT Noise Modulation
- Apply 2D DCT frequency dither to PNG/JPEG image assets to disrupt spatial frequency watermarks (SynthID style).

### 8. 📜 Automatic SHA-256 Audit Certificate Generation
- Compute SHA-256 verification hashes for zero-trust compliance transparency.

### 9. 🪝 Automatic Git Pre-Commit & CI/CD Shield
- Integrate with `.git/hooks/pre-commit` and `.github/workflows/untrace-hygiene.yml` to audit commits automatically.

### 10. 📋 Automatic System Clipboard Hygiene
- Clean system clipboard events in real time.

---

### Execution Guarantee
Every output and file created by you is ALREADY 100% clean, humanized, and untraceable BY DEFAULT. Zero user commands required.
